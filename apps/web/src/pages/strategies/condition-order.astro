---
import BaseLayout from "../../layouts/BaseLayout.astro";
---
<BaseLayout title="条件单策略说明 | 韭菜圈">
  <article class="mx-auto max-w-4xl px-4 py-10 leading-8">
    <p class="text-sm font-semibold text-leaf">策略说明</p>
    <h1 class="mt-2 text-3xl font-bold md:text-5xl">次日条件单参考价算法</h1>
    <p class="mt-6 text-stone-700">当前版本复用既有网格/波段辅助计算逻辑，用上一交易快照的最高、最低、收盘价格生成次日参考区间。</p>
    <div class="mt-8 rounded-lg border border-line bg-white p-6">
      <h2 class="text-xl font-bold">计算公式</h2>
      <ul class="mt-4 space-y-2 text-stone-700">
        <li><code>d = high - low</code></li>
        <li><code>a = (2 * close + high + low) / 4</code></li>
        <li><code>低吸价 = a - d</code></li>
        <li><code>高抛价 = a + d</code></li>
        <li><code>小观察价 = 2 * a - high</code></li>
        <li><code>大观察价 = 2 * a - low</code></li>
      </ul>
    </div>
    <div class="mt-8 grid gap-4 md:grid-cols-2">
      <section class="rounded-lg border border-line bg-white p-6">
        <h2 class="text-xl font-bold">适用场景</h2>
        <p class="mt-3 text-stone-700">适合有明确自选池、希望提前设置条件单、减少盘中情绪交易的用户。</p>
      </section>
      <section class="rounded-lg border border-line bg-white p-6">
        <h2 class="text-xl font-bold">不适用场景</h2>
        <p class="mt-3 text-stone-700">不适合追求确定性预测、短线追涨、或没有仓位计划的交易场景。</p>
      </section>
    </div>
    <p class="mt-8 rounded-lg bg-amber-50 p-5 text-sm text-amber-900">提示：工具输出的是策略计算结果，不是买卖建议。实际交易应结合自身风险承受能力、仓位和市场环境独立判断。</p>
  </article>
</BaseLayout>
